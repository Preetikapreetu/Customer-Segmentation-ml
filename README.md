# Customer-Segmentation-ml
A machine learning project that uses the K-Means clustering algorithm to segment customers based on annual income and spending score. Includes data visualization and an interactive Gradio interface.
